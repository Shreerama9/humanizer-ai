# Core ML
# PyTorch: install separately for your CUDA version — see README Quickstart
# RTX 50xx (Blackwell sm_120): pip install torch --index-url https://download.pytorch.org/whl/cu128
# RTX 30xx/40xx (sm_86/sm_89):  pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.7.0
transformers==4.44.2
peft==0.12.0
trl==0.9.6
bitsandbytes>=0.44.0
accelerate==0.33.0
datasets==2.21.0
tokenizers==0.19.1

# Flash Attention (CUDA 12.x, A100/H100 recommended)
# Install separately: pip install flash-attn --no-build-isolation
# flash-attn==2.6.3

# Training utilities
wandb==0.17.6
tensorboard==2.17.0

# Data pipeline
openai==1.40.0
tqdm==4.66.5
pyyaml==6.0.2

# API server
fastapi==0.112.1
uvicorn[standard]==0.30.6
pydantic==2.8.2
python-multipart==0.0.9
httpx==0.27.0

# Evaluation
spacy==3.7.5
# python -m spacy download en_core_web_sm

# Readability (pure Python, no model)
# All readability logic is implemented locally in evaluation/readability.py

# Misc
numpy==1.26.4
scipy==1.14.0
